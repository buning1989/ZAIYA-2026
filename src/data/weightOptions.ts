/* —— 体重记录模块配置数据 ——
 *
 * 与情绪 / 饮食模块一致的分页原则：每页只做一个判断。
 *
 * 体重模块不做成冷冰冰的数值录入，也不强化胖瘦评价。定位是帮助用户和支持系统
 * 看见身体状态变化，不在记录过程中制造体重焦虑。记录流程中不出现「偏胖 / 偏瘦 /
 * 正常 / 超重」等评价词，BMI 或体重区间解释放在「回头看看」中作为趋势参考。
 *
 * 结构：
 *   1. 测量场景（胶囊单选，含「记不清」）
 *   2. 补充说明（「没有补充」单选自动进入 / 「写一点」展开自由输入）
 *
 * 组件只读取本文件配置并渲染，不在此处写死 UI 逻辑。 */

/* —— 测量场景 ——
 * 单选，胶囊样式，选中后自动进入下一步。包含「记不清」弱选项。 */
export const weightMeasureContextOptions: string[] = [
  "起床后",
  "饭前",
  "饭后",
  "晚上",
  "随手称的",
  "记不清",
];
